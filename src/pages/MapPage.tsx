import PlaceholderPage from '../components/ui/PlaceholderPage'
export default function MapPage() {
  return <PlaceholderPage pageKey="map" title="Our Map" emoji="📍" description="every place we've been, pinned with a frog" />
}
